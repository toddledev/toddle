{
  "name": "Format Number",
  "arguments": [
    {
      "name": "Input",
      "description": "Number to format.",
      "type": { "type": "Number" },
      "formula": { "type": "Number", "value": 100 }
    },
    {
      "name": "Locale(s)",
      "description": "Optional locale to use for formatting the Number, e.g. \"en\" or \"fr\". Multiple locales can be provided (as an Array of Strings) to provide a fallback locale. The default value is the runtime's locale.",
      "type": { "type": "String | Array" },
      "formula": { "type": "value", "value": null }
    },
    {
      "name": "Options",
      "description": "Optional Object for configuring the formatting of the output.",
      "type": { "type": "Object" },
      "formula": { "type": "value", "value": null }
    }
  ],
  "description": "Format a Number using the Intl.NumberFormat API.",
  "output": {
    "description": "The Number input formatted as a String.",
    "type": { "type": "String" }
  }
}
